com.instana.plugin.generic.hardware:
  enabled: true
  availability-zone: "{{ instana_monitoring.zone }}-{{ group_names[0] }}"

com.instana.plugin.host:
  tags:
    - {{ instana_environment }}
    - {{ instana_monitoring.zone }}
    - {{ instana_agent_name }}
